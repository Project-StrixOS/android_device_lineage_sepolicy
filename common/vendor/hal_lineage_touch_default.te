type hal_strix_touch_default, domain;
hal_server_domain(hal_strix_touch_default, hal_strix_touch)

type hal_strix_touch_default_exec, exec_type, vendor_file_type, file_type;
init_daemon_domain(hal_strix_touch_default)

binder_call(hal_strix_touch_default, servicemanager)
